Resolve a NullAway warning by learning from safe usage patterns in the codebase. **Error:** At line node.next.prev = node.prev;, the dereferenced expression `node.next` is and can cause a NullPointerException. **Method with error:** void removeInternal(Node<K, V> node, boolean unlink) { if (unlink) { node.prev.next = node.next; node.next.prev = node.prev; node.next = node.prev = null; // Help the GC (for performance) } Node<K, V> left = node.left; Node<K, V> right = node.right; Node<K, V> originalParent = node.parent; if (left != null && right != null) { /* * To remove a node with both left and right subtrees, move an * adjacent node from one of those subtrees into this node's place. * * Removing the adjacent node may change this node's subtrees. This * node may no longer have two subtrees once the adjacent node is * gone! */ Node<K, V> adjacent = (left.height > right.height) ? left.last() : right.first(); removeInternal(adjacent, false); // takes care of rebalance and size-- int leftHeight = 0; left = node.left; if (left != null) { leftHeight = left.height; adjacent.left = left; left.parent = adjacent; node.left = null; } int rightHeight = 0; right = node.right; if (right != null) { rightHeight = right.height; adjacent.right = right; right.parent = adjacent; node.right = null; } adjacent.height = Math.max(leftHeight, rightHeight) + 1; replaceInParent(node, adjacent); return; } else if (left != null) { replaceInParent(node, left); node.left = null; } else if (right != null) { replaceInParent(node, right); node.right = null; } else { replaceInParent(node, null); } rebalance(originalParent, false); size--; modCount++; } **Safe usage examples from the codebase:** The examples below show where the same dereferenced expression is used safely without causing NullPointerExceptions: Node() { key = null; hash = -1; next = prev = this; } Node(Node<K, V> parent, K key, int hash, Node<K, V> next, Node<K, V> prev) { this.parent = parent; this.key = key; this.hash = hash; this.height = 1; this.next = next; this.prev = prev; prev.next = this; next.prev = this; } ### Task: Analyze the safe usage patterns in the examples above and apply the same pattern to fix the error in the original method. The examples demonstrate how the codebase typically handles this nullable expression safely. ### Requirements: - Use the same null-handling pattern shown in the examples - Maintain consistency with the existing codebase style - Do not import any additional libraries or classes - Do not add Java comments, but also do not remove any preexisting comments. - Only modify relevant lines as required - Do not address any other issues - Return only the modified method ### Response Format: **If you can apply the pattern from the examples:** <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response> **If you cannot apply the pattern (e.g., examples don't provide a clear solution):** <response> <success>false</success> </response>