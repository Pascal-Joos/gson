Rewrite the method below to handle the nullable dereference: ```java @SuppressWarnings("unchecked") // guarded by typeToken.equals() call @Override public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) { boolean matches = exactType != null ? exactType.equals(type) || matchRawType && exactType.getType() == type.getRawType() : hierarchyType.isAssignableFrom(type.getRawType()); return matches ? new TreeTypeAdapter<T>( (JsonSerializer<T>) serializer, (JsonDeserializer<T>) deserializer, gson, type, this) : null; } ``` At line " : hierarchyType.isAssignableFrom(type.getRawType());", the dereferenced expression "hierarchyType" is . Apply the following fix strategy: 1. Add a null check closest to the dereference point 2. If null, return null from the method 3. Add the annotation to the method declaration to indicate it can now return null ### Requirements: - Do not import any additional libraries or classes - Do not add Java comments, but also do not remove any preexisting comments. - Only modify relevant lines as required - Do not address any other issues - Return only the modified method ### Response Format: <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>