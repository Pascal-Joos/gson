In the method below, the expression In the provided method: public char charAt(int i) { return new String(chars, i, 1).charAt(0); } the expression on the return line that could be under analysis is "chars" (the array passed to the String constructor). Key points: 1. The method returns a primitive char, not a Character object. A primitive char cannot be null by definition in Java. 2. The return expression does not directly use "chars[i]"; instead it constructs a new String from the array and then calls charAt(0) on that String: - new String(chars, i, 1) will throw: - NullPointerException if chars is null - StringIndexOutOfBoundsException if i is out of range - If this constructor returns normally, the resulting String is guaranteed non-null. - String.charAt(0) on a valid String returns a primitive char, which cannot be null. 3. Therefore: - If "chars" is null, the constructor will throw NullPointerException before any char value is produced. No null value is ever returned; the call simply fails. - If "chars" is not null and the index is valid, a non-null String is created and a primitive char is returned. There is no execution path where the expression ultimately producing the return value could yield a null, because the return type is primitive char and the String API guarantees a non-null String from the constructor when it does not throw. Thus, under Java’s type system and the semantics of String’s constructor and charAt, the value returned from this line cannot be null; attempts to use a null "chars" would cause an exception, not a null char. is not nullable at the point it is used. ```java chars ``` Please rewrite the method so that at the line "public char charAt(int i) { return new String(chars, i, 1).charAt(0); }", at the occurrence of: " return chars[i];" is replaced with `Nullability.castToNonnull(chars)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>